import { NextRequest, NextResponse } from "next/server";
import { getCurrentUserFromCookie } from "@/lib/auth";
import { readWabDB, writeWabDB } from "@/lib/wab-db";
export async function GET(_request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const db = readWabDB(); const salon = db.salons.find((item) => item.id === id); if (!salon) return NextResponse.json({ error: "Salon introuvable." }, { status: 404 }); return NextResponse.json({ salon, participants: db.salonParticipants.filter((item) => item.salonId === id), messages: db.salonMessages.filter((item) => item.salonId === id).slice(-100) }); }
export async function PATCH(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const user = await getCurrentUserFromCookie(); const { id } = await params; if (!user) return NextResponse.json({ error: "Connexion requise." }, { status: 401 }); const db = readWabDB(); const salon = db.salons.find((item) => item.id === id && item.hostUserId === user.id); if (!salon) return NextResponse.json({ error: "Action réservée à l’animateur." }, { status: 403 }); const { status, replayUrl } = await request.json(); if (!["scheduled", "live", "ended", "cancelled"].includes(status)) return NextResponse.json({ error: "Statut invalide." }, { status: 400 }); salon.status = status; if (status === "ended") salon.endsAt = new Date().toISOString(); if (typeof replayUrl === "string") salon.replayUrl = replayUrl.slice(0, 1000); writeWabDB(db); return NextResponse.json({ salon }); }
