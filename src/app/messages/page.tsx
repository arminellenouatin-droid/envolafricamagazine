"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";

type MessageItem = { id: string; platform: string; threadId: string | null; senderId: string | null; recipientId: string; body: string; subject: string | null; href: string | null; readAt: string | null; createdAt: string };
const labels: Record<string, string> = { wab: "WAB", marketplace: "Marketplace", crowdfunding: "Crowdfunding", jobs: "Jobs", awards: "Africa Awards", system: "Envol Africa" };

export default function MessagesPage() {
  const [items, setItems] = useState<MessageItem[]>([]);
  const [filter, setFilter] = useState("all");
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");
  const load = async () => { setLoading(true); try { const response = await fetch("/api/messages", { cache: "no-store" }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Impossible de charger les messages."); setItems(data.messages || []); } catch (err) { setError(err instanceof Error ? err.message : "Impossible de charger les messages."); } finally { setLoading(false); } };
  useEffect(() => { load(); }, []);
  const markRead = async () => { await fetch("/api/messages", { method: "PATCH" }); setItems((current) => current.map((item) => ({ ...item, readAt: item.readAt || new Date().toISOString() }))); };
  const visible = useMemo(() => filter === "all" ? items : items.filter((item) => item.platform === filter), [filter, items]);
  const unread = items.filter((item) => !item.readAt).length;
  const platforms = Array.from(new Set(items.map((item) => item.platform)));
  return <main className="min-h-screen bg-[#fcf9f8] px-4 pb-24 pt-8 text-[#221b1b] dark:bg-[#161415] dark:text-[#f8eeee] sm:px-6 lg:px-10"><div className="mx-auto max-w-5xl"><div className="flex flex-col justify-between gap-4 border-b border-[#e5bdbb] pb-6 sm:flex-row sm:items-end"><div><p className="text-[11px] font-black uppercase tracking-[0.18em] text-[#9e001f]">Conversations de l’écosystème</p><h1 className="mt-2 font-display text-3xl font-black">Messages</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[#6b5353] dark:text-[#d7c2c2]">Retrouvez vos échanges WAB, Marketplace, Crowdfunding et les messages liés à vos activités.</p></div><button type="button" onClick={markRead} disabled={!unread} className="h-11 rounded-full bg-[#9e001f] px-5 text-xs font-black text-white transition hover:bg-[#c8102e] disabled:cursor-not-allowed disabled:opacity-40">Tout marquer comme lu</button></div><div className="mt-6 flex flex-wrap gap-2"><button type="button" onClick={() => setFilter("all")} className={`rounded-full border px-4 py-2 text-xs font-bold ${filter === "all" ? "border-[#9e001f] bg-[#9e001f] text-white" : "border-[#e5bdbb] bg-white dark:bg-[#241d1f]"}`}>Tous {unread > 0 && <span className="ml-1">({unread})</span>}</button>{platforms.map((platform) => <button key={platform} type="button" onClick={() => setFilter(platform)} className={`rounded-full border px-4 py-2 text-xs font-bold ${filter === platform ? "border-[#9e001f] bg-[#9e001f] text-white" : "border-[#e5bdbb] bg-white dark:bg-[#241d1f]"}`}>{labels[platform] || platform}</button>)}</div>{error && <div className="mt-6 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800">{error}</div>}{loading ? <div className="mt-8 rounded-2xl border border-[#e5bdbb] bg-white p-8 text-sm text-[#6b5353] dark:bg-[#241d1f]">Chargement des messages...</div> : visible.length === 0 ? <div className="mt-8 rounded-2xl border border-dashed border-[#d8c3c1] bg-white p-10 text-center dark:bg-[#241d1f]"><span className="material-symbols-outlined text-4xl text-[#9e001f]">mail</span><h2 className="mt-3 font-display text-lg font-black">Aucun message reçu</h2><p className="mt-2 text-sm text-[#6b5353] dark:text-[#d7c2c2]">Les nouveaux échanges apparaîtront ici dès qu’ils seront disponibles.</p></div> : <div className="mt-8 divide-y divide-[#ead9d7] overflow-hidden rounded-2xl border border-[#e5bdbb] bg-white dark:divide-[#443237] dark:bg-[#241d1f]">{visible.map((item) => <Link key={`${item.platform}-${item.id}`} href={item.href || "/messages"} className={`flex gap-4 p-5 transition hover:bg-[#fff5f3] dark:hover:bg-[#302628] ${!item.readAt ? "bg-[#fff8f6] dark:bg-[#2b2224]" : ""}`}><span className={`mt-1 grid h-10 w-10 shrink-0 place-items-center rounded-full ${!item.readAt ? "bg-[#9e001f] text-white" : "bg-[#f2e8e6] text-[#9e001f]"}`}><span className="material-symbols-outlined text-[20px]">mail</span></span><span className="min-w-0 flex-1"><span className="flex flex-wrap items-center gap-2"><strong className="font-display text-sm font-black">{item.subject || `Message ${labels[item.platform] || item.platform}`}</strong>{!item.readAt && <span className="h-2 w-2 rounded-full bg-[#9e001f]" aria-label="Non lu" />}</span><span className="mt-1 block truncate text-sm leading-6 text-[#6b5353] dark:text-[#d7c2c2]">{item.body}</span><span className="mt-2 block text-[11px] font-bold uppercase tracking-wider text-[#9e001f]">{labels[item.platform] || item.platform} · {new Date(item.createdAt).toLocaleString("fr-FR")}</span></span><span className="material-symbols-outlined mt-2 text-[#aa8f8f]">chevron_right</span></Link>)}</div>}</div></main>;
}
