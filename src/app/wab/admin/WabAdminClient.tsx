"use client";
import { useEffect, useState } from "react";
type Post = { id: string; author: string; content: string; moderationStatus: string }; type Report = { id: string; targetType: string; reason: string; status: string }; type Profile = { id: string; fullName: string; headline: string; status: string }; type Reward = { id: string; type: string; threshold: number; amount: number; status: string };
export default function WabAdminClient() { const [posts, setPosts] = useState<Post[]>([]); const [reports, setReports] = useState<Report[]>([]); const [profiles, setProfiles] = useState<Profile[]>([]); const [rewards, setRewards] = useState<Reward[]>([]); const [tab, setTab] = useState("posts"); const [error, setError] = useState(""); useEffect(() => { fetch("/api/wab/admin").then(async (response) => { if (!response.ok) throw new Error("Accès administrateur requis."); return response.json(); }).then((data) => { setPosts(data.posts); setReports(data.reports); setProfiles(data.profiles); setRewards(data.rewards ?? []); }).catch((cause) => setError(cause.message)); }, []); async function update(targetType: string, targetId: string, status: string) { const response = await fetch("/api/wab/admin", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ targetType, targetId, status }) }); if (!response.ok) { setError("Mise à jour impossible."); return; } if (targetType === "post") setPosts((items) => items.map((item) => item.id === targetId ? { ...item, moderationStatus: status } : item)); if (targetType === "report") setReports((items) => items.map((item) => item.id === targetId ? { ...item, status } : item)); if (targetType === "profile") setProfiles((items) => items.map((item) => item.id === targetId ? { ...item, status } : item)); if (targetType === "reward") setRewards((items) => items.map((item) => item.id === targetId ? { ...item, status } : item)); }
 const tabs = [{ id: "posts", label: "Publications" }, { id: "reports", label: "Signalements" }, { id: "profiles", label: "Comptes WAB" }, { id: "rewards", label: "Récompenses" }]; return <main className="min-h-screen bg-[#f4f7f8] py-10"><div className="mx-auto max-w-6xl px-5"><p className="text-xs font-bold uppercase tracking-widest text-[#087e8b]">Administration WAB</p><h1 className="mt-2 font-display text-3xl font-extrabold text-[#082843]">Modération professionnelle</h1><p className="mt-3 text-slate-600">Les actions prises ici s’appliquent uniquement à World Africa Business.</p>{error && <p className="mt-5 rounded-xl bg-red-50 p-4 text-sm text-red-800">{error}</p>}<div className="mt-7 flex flex-wrap gap-2">{tabs.map((item) => <button key={item.id} onClick={() => setTab(item.id)} className={`rounded-xl px-4 py-3 text-sm font-bold ${tab === item.id ? "bg-[#082843] text-white" : "bg-white text-slate-600"}`}>{item.label}</button>)}</div>{tab === "posts" && <section className="mt-5 space-y-3">{posts.map((post) => <article key={post.id} className="rounded-2xl bg-white p-5 shadow-sm"><strong>{post.author}</strong><p className="mt-2 text-sm text-slate-600">{post.content}</p><div className="mt-4 flex flex-wrap items-center gap-3"><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{post.moderationStatus}</span>{["published", "hidden", "rejected"].map((status) => <button key={status} onClick={() => update("post", post.id, status)} className="text-xs font-bold text-[#087e8b]">{status}</button>)}</div></article>)}</section>}{tab === "reports" && <section className="mt-5 space-y-3">{reports.map((report) => <article key={report.id} className="rounded-2xl bg-white p-5 shadow-sm"><strong>Signalement {report.targetType}</strong><p className="mt-2 text-sm text-slate-600">{report.reason}</p><div className="mt-4 flex gap-3"><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{report.status}</span>{["reviewing", "resolved", "dismissed"].map((status) => <button key={status} onClick={() => update("report", report.id, status)} className="text-xs font-bold text-[#087e8b]">{status}</button>)}</div></article>)}</section>}{tab === "profiles" && <section className="mt-5 space-y-3">{profiles.map((profile) => <article key={profile.id} className="rounded-2xl bg-white p-5 shadow-sm"><strong>{profile.fullName}</strong><p className="mt-1 text-sm text-slate-600">{profile.headline}</p><div className="mt-4 flex gap-3"><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{profile.status}</span>{["active", "silent", "banned"].map((status) => <button key={status} onClick={() => update("profile", profile.id, status)} className="text-xs font-bold text-[#087e8b]">{status}</button>)}</div></article>)}</section>}{tab === "rewards" && <section className="mt-5 space-y-3">{rewards.map((reward) => <article key={reward.id} className="rounded-2xl bg-white p-5 shadow-sm"><strong>{reward.type === "views_1000" ? "1 000 vues éligibles" : "3 000 minutes vidéo"}</strong><p className="mt-1 text-sm text-slate-600">Palier n° {reward.threshold} · {reward.amount.toLocaleString("fr-FR")} XOF</p><div className="mt-4 flex gap-3"><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{reward.status}</span>{["validated", "rejected", "paid"].map((status) => <button key={status} onClick={() => update("reward", reward.id, status)} className="text-xs font-bold text-[#087e8b]">{status}</button>)}</div></article>)}</section>}</div></main>; }
