"use client";
import { useState } from "react";
export default function BoostButton({ targetId, targetType, offerPosition = 1 }: { targetId: string; targetType: "offer" | "candidate"; offerPosition?: number }) { const [days, setDays] = useState(7); const [loading, setLoading] = useState(false); async function boost() { setLoading(true); try { const response = await fetch("/api/jobs/boosts", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ targetId, targetType, days, offerPosition }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); window.location.assign(data.checkoutUrl); } catch (error) { alert(error instanceof Error ? error.message : "Erreur de boost"); setLoading(false); } } return <div className="mt-3 flex items-center gap-2"><select value={days} onChange={(event) => setDays(Number(event.target.value))} className="rounded-lg border border-slate-200 px-2 py-2 text-xs"><option value={1}>1 jour</option><option value={7}>7 jours</option><option value={10}>10 jours</option><option value={15}>15 jours</option></select><button onClick={boost} disabled={loading} className="rounded-lg bg-[#f6c453] px-3 py-2 text-xs font-extrabold text-slate-900 disabled:opacity-60">{loading ? "…" : "★ Booster"}</button></div>; }
