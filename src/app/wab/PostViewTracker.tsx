"use client";
import { useEffect, useRef } from "react";
export default function PostViewTracker({ postId }: { postId: string }) { const ref = useRef<HTMLDivElement>(null); useEffect(() => { const node = ref.current; if (!node) return; let recorded = false; const observer = new IntersectionObserver(([entry]) => { if (!recorded && entry.isIntersecting && entry.intersectionRatio >= 0.55) { recorded = true; const visitorId = localStorage.getItem("ea_visitor_id") || crypto.randomUUID(); localStorage.setItem("ea_visitor_id", visitorId); fetch(`/api/wab/posts/${postId}/view`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ visitorId, watchSeconds: 0 }) }).catch(() => undefined); observer.disconnect(); } }, { threshold: [0.55] }); observer.observe(node); return () => observer.disconnect(); }, [postId]); return <div ref={ref} className="h-px" aria-hidden="true"/>; }
