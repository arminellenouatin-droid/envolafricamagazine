"use client";
import { useEffect, useState } from "react";

type NotificationItem = { id: string; title: string; body: string; href?: string; readAt?: string | null; createdAt: string };
export default function NotificationsPage() { const [items, setItems] = useState<NotificationItem[]>([]); const [error, setError] = useState(""); useEffect(() => { fetch("/api/wab/notifications").then((r) => r.json()).then((d) => setItems(d.notifications || [])).catch(() => setError("Notifications indisponibles.")); fetch("/api/wab/notifications", { method: "PATCH" }).catch(() => undefined); }, []); return <main className="min-h-screen bg-[#e9f7f5] px-4 py-8"><div className="mx-auto max-w-2xl"><a href="/wab" className="text-sm font-bold text-[#006874]">← Retour au fil</a><h1 className="mt-5 font-display text-3xl font-extrabold text-[#082843]">Notifications</h1>{error && <p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="mt-6 space-y-3">{items.length ? items.map((item) => <a key={item.id} href={item.href || "/wab"} className={`block rounded-2xl border p-4 shadow-sm ${item.readAt ? "border-[#d1e9e6] bg-white" : "border-[#f3b7bd] bg-[#fff7f7]"}`}><p className="font-bold text-[#082843]">{item.title}</p><p className="mt-1 text-sm text-slate-600">{item.body}</p><time className="mt-2 block text-[11px] text-slate-400">{new Date(item.createdAt).toLocaleString("fr-FR")}</time></a>) : <p className="rounded-2xl bg-white p-6 text-sm text-slate-500">Aucune notification.</p>}</div></div></main>; }
