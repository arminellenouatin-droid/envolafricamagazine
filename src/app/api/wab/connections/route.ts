import { NextRequest, NextResponse } from "next/server";
import { getCurrentUserFromCookie } from "@/lib/auth";
import { readWabDB, writeWabDB } from "@/lib/wab-db";
export async function GET() { const user = await getCurrentUserFromCookie(); if (!user) return NextResponse.json({ connections: [] }); const db = readWabDB(); const connections = db.connections.filter((item) => item.followerUserId === user.id).map((item) => db.profiles.find((profile) => profile.id === item.profileId)).filter(Boolean); return NextResponse.json({ connections }); }
export async function POST(request: NextRequest) { const user = await getCurrentUserFromCookie(); if (!user) return NextResponse.json({ error: "Connexion requise." }, { status: 401 }); const { profileId } = await request.json(); const db = readWabDB(); const profile = db.profiles.find((item) => item.id === profileId && item.status === "active"); if (!profile || profile.userId === user.id) return NextResponse.json({ error: "Profil indisponible." }, { status: 400 }); const index = db.connections.findIndex((item) => item.followerUserId === user.id && item.profileId === profileId); let following; if (index >= 0) { db.connections.splice(index, 1); following = false; } else { db.connections.push({ followerUserId: user.id, profileId, createdAt: new Date().toISOString() }); following = true; } writeWabDB(db); return NextResponse.json({ following }); }
