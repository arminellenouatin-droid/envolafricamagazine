"use client";
import { useState } from "react";
export default function ConnectButton({ profileId }: { profileId: string }) { const [following, setFollowing] = useState(false); async function toggle() { const response = await fetch("/api/wab/connections", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ profileId }) }); if (response.status === 401) { window.location.assign(`/auth/login?next=${encodeURIComponent("/wab/recherche")}`); return; } const data = await response.json(); if (response.ok) setFollowing(data.following); } return <button onClick={toggle} className={`mt-4 rounded-lg px-3 py-2 text-xs font-bold ${following ? "bg-slate-100 text-slate-600" : "bg-[#087e8b] text-white"}`}>{following ? "Ne plus suivre" : "Suivre"}</button>; }
