"use client";
import Link from "next/link";
import { useState } from "react";
import { AFRICA_COUNTRIES } from "@/lib/africa-context";
const countries = AFRICA_COUNTRIES.map((country) => country.name);
export default function OfferForm() { const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setMessage(""); const form = new FormData(event.currentTarget); const payload: Record<string, unknown> = Object.fromEntries(form.entries()); payload.skills = String(form.get("skills") || "").split(",").map((item) => item.trim()).filter(Boolean); try { const response = await fetch("/api/jobs", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const data = await response.json(); if (response.status === 401) { window.location.assign(`/auth/login?next=${encodeURIComponent("/emploi/publier-offre")}`); return; } if (response.status === 402) { setMessage(""); window.location.assign("/emploi/abonnements"); return; } if (!response.ok) throw new Error(data.error); setMessage("Votre offre est publiée. Les coordonnées de votre entreprise sont protégées jusqu’au décryptage par les candidats."); event.currentTarget.reset(); } catch (error) { setMessage(error instanceof Error ? error.message : "Une erreur est survenue."); } finally { setBusy(false); } } const field = "mt-1 w-full rounded-xl border border-slate-200 px-3 py-3 outline-none focus:border-[#087e8b]"; return <main className="min-h-screen bg-[#f7f8fa] py-10"><div className="mx-auto max-w-3xl px-5"><p className="text-xs font-bold uppercase tracking-widest text-[#a36300]">Entreprise · deux offres offertes</p><h1 className="mt-2 font-display text-3xl font-extrabold text-[#071b36]">Publiez une offre d’emploi</h1><p className="mt-3 text-slate-600">Les deux premières publications sont gratuites. Les coordonnées de votre structure restent protégées jusqu’au décryptage par un candidat.</p><form onSubmit={submit} className="mt-7 rounded-3xl bg-white p-6 shadow-sm sm:p-8"><div className="grid gap-5 sm:grid-cols-2"><label>Intitulé du poste<input name="title" required className={field}/></label><label>Secteur<input name="sector" required placeholder="Tech, Finance, Agro…" className={field}/></label><label>Type de contrat<select name="contractType" required className={field}><option>CDI</option><option>CDD</option><option>Stage</option><option>Freelance</option><option>Remote</option></select></label><label>Rémunération<input name="salary" placeholder="Ex. Selon profil" className={field}/></label><label>Nom de l’entreprise<input name="companyName" required className={field}/></label><label>E-mail de contact<input name="contactEmail" required type="email" className={field}/></label><label>Téléphone<input name="contactPhone" type="tel" className={field}/></label><label>Adresse<input name="address" className={field}/></label><label>Pays<select name="country" required className={field}><option value="">Choisir</option>{countries.map((country) => <option key={country}>{country}</option>)}</select></label><label>Ville<input name="city" required className={field}/></label><label>Date de fin<input name="expiresAt" type="date" required className={field}/></label></div><label className="mt-5 block">Compétences <span className="text-xs text-slate-400">(séparées par des virgules)</span><input name="skills" className={field}/></label><label className="mt-5 block">Description de l’offre<textarea name="description" required rows={7} className={field}/></label>{message && <p className="mt-5 rounded-xl bg-[#e9f7f5] p-4 text-sm font-semibold text-[#087e8b]">{message}</p>}<div className="mt-6 flex flex-wrap gap-4"><button disabled={busy} className="rounded-xl bg-[#087e8b] px-6 py-3 font-bold text-white disabled:opacity-60">{busy ? "Publication…" : "Publier l’offre"}</button><Link href="/emploi/abonnements" className="rounded-xl border border-slate-300 px-6 py-3 text-sm font-bold text-slate-700">Voir les accès entreprise</Link></div></form></div></main>; }
