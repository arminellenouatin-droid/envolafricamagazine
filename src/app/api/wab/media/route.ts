import { NextRequest, NextResponse } from "next/server";
import { createClient } from "@supabase/supabase-js";
import { readWabDB } from "@/lib/wab-db";
export async function GET(request: NextRequest) { const postId = request.nextUrl.searchParams.get("postId"); const index = Number(request.nextUrl.searchParams.get("index") ?? "0"); if (!postId || !Number.isInteger(index) || index < 0) return NextResponse.json({ error: "Média invalide." }, { status: 400 }); const post = readWabDB().posts.find((item) => item.id === postId && item.moderationStatus === "published"); const media = post?.media?.[index]; if (!media) return NextResponse.json({ error: "Média indisponible." }, { status: 404 }); const key = process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.SUPABASE_SECRET_KEY; const url = process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL; if (!key || !url) return NextResponse.json({ error: "Stockage indisponible." }, { status: 503 }); const supabase = createClient(url, key, { auth: { persistSession: false } }); const { data, error } = await supabase.storage.from("wab-media").createSignedUrl(media.path, 300); if (error || !data?.signedUrl) return NextResponse.json({ error: "Impossible de préparer ce média." }, { status: 502 }); return NextResponse.json({ url: data.signedUrl, mimeType: media.mimeType, name: media.name }); }
