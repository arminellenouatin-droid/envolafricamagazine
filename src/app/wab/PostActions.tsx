"use client";
import { useState } from "react";
export default function PostActions({ postId, initialLikes, comments }: { postId: string; initialLikes: number; comments: number }) { const [likes, setLikes] = useState(initialLikes); const [liked, setLiked] = useState(false); const [notice, setNotice] = useState(""); const [showBoost, setShowBoost] = useState(false); const [budget, setBudget] = useState(1000); const [days, setDays] = useState(7); const [countries, setCountries] = useState(""); const [industries, setIndustries] = useState(""); const [boosting, setBoosting] = useState(false); async function react() { const response = await fetch(`/api/wab/posts/${postId}/reaction`, { method: "POST" }); const data = await response.json(); if (response.status === 401) { window.location.assign(`/auth/login?next=${encodeURIComponent("/wab")}`); return; } if (response.ok) { setLiked(data.liked); setLikes(data.likes); } } async function report() { const reason = window.prompt("Pourquoi signalez-vous cette publication ?"); if (!reason) return; const response = await fetch("/api/wab/reports", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ targetType: "post", targetId: postId, reason }) }); setNotice(response.ok ? "Signalement transmis à l’équipe WAB." : "Signalement impossible."); } async function boost() { setBoosting(true); const response = await fetch("/api/wab/boosts", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ postId, budgetXof: budget, durationDays: days, targetCountries: countries.split(",").map((value) => value.trim()).filter(Boolean), targetIndustries: industries.split(",").map((value) => value.trim()).filter(Boolean) }) }); const data = await response.json(); if (response.status === 401) { window.location.assign(`/auth/login?next=${encodeURIComponent("/wab")}`); return; } if (!response.ok) { setNotice(data.error || "Promotion impossible."); setBoosting(false); return; } window.location.assign(data.checkoutUrl); } return <div className="mt-5 border-t pt-4"><div className="flex flex-wrap items-center gap-5 text-sm text-slate-500"><button onClick={react} className={`font-semibold ${liked ? "text-[#c91f3b]" : ""}`}>{liked ? "♥" : "♡"} {likes}</button><span>◌ {comments} commentaires</span><button onClick={() => setShowBoost(!showBoost)} className="text-xs font-bold text-[#087e8b]">Promouvoir</button><button onClick={report} className="ml-auto text-xs font-bold text-slate-400 hover:text-red-700">Signaler</button></div>{showBoost && <div className="mt-3 grid gap-2 rounded-xl bg-[#fff3dc] p-3 sm:grid-cols-[1fr_120px_auto]"><label className="text-xs font-bold text-slate-600">Budget XOF<input min="1" type="number" value={budget} onChange={(event) => setBudget(Number(event.target.value))} className="mt-1 w-full rounded-lg border p-2 text-sm"/></label><label className="text-xs font-bold text-slate-600">Durée (jours)<input min="1" max="90" type="number" value={days} onChange={(event) => setDays(Number(event.target.value))} className="mt-1 w-full rounded-lg border p-2 text-sm"/></label><button disabled={boosting} onClick={boost} className="self-end rounded-lg bg-[#a36300] px-3 py-2 text-xs font-bold text-white">{boosting ? "…" : "Payer et promouvoir"}</button><label className="sm:col-span-3 text-xs font-bold text-slate-600">Pays ciblés <span className="font-normal">(séparés par des virgules, facultatif)</span><input value={countries} onChange={(event) => setCountries(event.target.value)} placeholder="Bénin, Sénégal, Kenya" className="mt-1 w-full rounded-lg border p-2 text-sm"/></label><label className="sm:col-span-3 text-xs font-bold text-slate-600">Secteurs ciblés <span className="font-normal">(facultatif)</span><input value={industries} onChange={(event) => setIndustries(event.target.value)} placeholder="Finance, Tech, Agro" className="mt-1 w-full rounded-lg border p-2 text-sm"/></label></div>}{notice && <p className="mt-3 text-xs font-semibold text-[#087e8b]">{notice}</p>}</div>; }
