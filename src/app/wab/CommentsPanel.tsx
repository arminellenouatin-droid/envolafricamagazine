"use client";
import { useEffect, useState } from "react";
type Comment = { id: string; author: string; content: string; createdAt: string };
export default function CommentsPanel({ postId }: { postId: string }) { const [open, setOpen] = useState(false); const [items, setItems] = useState<Comment[]>([]); const [content, setContent] = useState(""); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); useEffect(() => { if (!open) return; fetch(`/api/wab/posts/${postId}/comments`).then((response) => response.json()).then((data) => setItems(data.comments ?? [])).catch(() => undefined); }, [open, postId]); async function add() { if (!content.trim()) return; setBusy(true); setError(""); try { const response = await fetch(`/api/wab/posts/${postId}/comments`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ content }) }); const data = await response.json(); if (response.status === 401) { window.location.assign(`/auth/login?next=${encodeURIComponent("/wab")}`); return; } if (!response.ok) throw new Error(data.error); setItems((values) => [...values, data.comment]); setContent(""); } catch (cause) { setError(cause instanceof Error ? cause.message : "Commentaire impossible."); } finally { setBusy(false); } } return <div className="mt-3"><button onClick={() => setOpen(!open)} className="text-xs font-bold text-[#087e8b]">{open ? "Masquer les commentaires" : "Voir et commenter"}</button>{open && <div className="mt-3 rounded-xl bg-slate-50 p-4"><div className="space-y-3">{items.map((item) => <div key={item.id} className="text-sm"><strong>{item.author}</strong><p className="mt-1 text-slate-600">{item.content}</p></div>)}{!items.length && <p className="text-sm text-slate-500">Aucun commentaire pour le moment.</p>}</div><div className="mt-4 flex gap-2"><input value={content} onChange={(event) => setContent(event.target.value)} placeholder="Ajouter une contribution professionnelle…" className="min-w-0 flex-1 rounded-lg border border-slate-200 px-3 py-2 text-sm"/><button disabled={busy} onClick={add} className="rounded-lg bg-[#087e8b] px-3 py-2 text-xs font-bold text-white">Envoyer</button></div>{error && <p className="mt-2 text-xs text-red-700">{error}</p>}</div>}</div>; }
